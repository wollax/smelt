---
phase: 10-real-agent-sessions
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - crates/smelt-core/src/orchestrate/executor.rs
  - crates/smelt-core/src/session/runner.rs
  - crates/smelt-cli/src/commands/orchestrate.rs
  - crates/smelt-core/src/lib.rs
autonomous: true
source_issue: ""

must_haves:
  truths:
    - Orchestrator dispatches to AgentExecutor when session.script is None
    - AgentExecutor receives task, file_scope, timeout, model, cancel token, and log path
    - Preflight check verifies `claude` binary exists before orchestration starts
    - CLI surfaces agent-related errors clearly (not found, timeout, killed)
    - SessionRunner also dispatches to AgentExecutor for standalone `smelt session run`
  artifacts:
    - Modified crates/smelt-core/src/orchestrate/executor.rs
    - Modified crates/smelt-core/src/session/runner.rs
    - Modified crates/smelt-cli/src/commands/orchestrate.rs
  key_links:
    - Orchestrator.execute_sessions() spawns AgentExecutor for script=None sessions
    - AgentExecutor.execute() returns SessionResult -> maps to SessionRunState
    - resolve_claude_binary() called in preflight before worktree creation
---

<objective>
Wire AgentExecutor into the orchestrator dispatch loop and CLI preflight.

Purpose: Connect the AgentExecutor from Plan 01 to the existing orchestration engine so that sessions without a `script` field launch a real Claude Code agent instead of returning an immediate Completed result.

Output: Modified orchestrator executor, session runner, and CLI command handler.
</objective>

<execution_context>
<!-- Executor agent has built-in instructions for plan execution and summary creation -->
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/pending/10-real-agent-sessions/10-RESEARCH.md
@.planning/phases/pending/10-real-agent-sessions/10-CONTEXT.md

@crates/smelt-core/src/orchestrate/executor.rs
@crates/smelt-core/src/session/runner.rs
@crates/smelt-core/src/session/agent.rs
@crates/smelt-core/src/session/types.rs
@crates/smelt-core/src/session/manifest.rs
@crates/smelt-cli/src/commands/orchestrate.rs
@crates/smelt-core/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire AgentExecutor into Orchestrator.execute_sessions()</name>
  <files>
    crates/smelt-core/src/orchestrate/executor.rs
    crates/smelt-core/src/lib.rs
  </files>
  <action>
Modify `Orchestrator::execute_sessions()` in `crates/smelt-core/src/orchestrate/executor.rs`:

1. **Add preflight check:** Add a method `check_agent_preflight(manifest: &Manifest) -> Result<Option<PathBuf>>` on Orchestrator that:
   - Scans manifest sessions for any that have `script: None` (i.e., agent sessions)
   - If any exist, calls `resolve_claude_binary()` and returns the PathBuf
   - If none exist, returns None (skip the check — scripted-only manifests don't need claude)
   - Call this in `run()` before Phase 1 (worktree creation). On error, return immediately.

2. **Modify the session dispatch block** in `execute_sessions()`. Currently the code has:
   ```rust
   } else {
       // No script — immediately complete
       Ok(SessionRunState::Completed {
           duration_secs: timer.elapsed().as_secs_f64(),
       })
   };
   ```

   Replace this `else` branch with agent execution:
   - Create an `AgentExecutor::new(worktree_path, log_path, timeout)`
     - `timeout`: derive from `session_def.timeout_secs` -> `Duration::from_secs(t)`
     - `log_path`: already available as `log_path` in the spawn closure
   - Resolve the task: `session_def.task` or read from `session_def.task_file` (task_file: read the file content as the task string)
   - Call `executor.execute(session_name, task, file_scope, model, child_cancel)`
     - `file_scope`: `session_def.file_scope.as_deref()` — convert `Option<Vec<String>>` to `Option<&[String]>`
     - `model`: None for v0.1.0 (or read from env config if trivially available — Claude's discretion)
   - Map `SessionResult` to `SessionRunState` the same way scripted sessions are mapped:
     - Completed -> SessionRunState::Completed
     - Failed -> SessionRunState::Failed
     - TimedOut -> SessionRunState::Failed with reason "session timed out"
     - Killed -> SessionRunState::Cancelled

3. **Pass claude_binary_path** into the spawn closure. The preflight returns `Option<PathBuf>`. Clone it into each agent spawn. AgentExecutor needs to know where `claude` is — either:
   - Option A: AgentExecutor takes the binary path in the constructor
   - Option B: AgentExecutor always resolves via `which::which` internally

   Use Option A for efficiency (resolve once, use many). Add `claude_binary: Option<PathBuf>` parameter. Update AgentExecutor's constructor if needed.

4. **Ensure task_file resolution happens before spawn.** If `session_def.task_file` is Some, read the file content synchronously before spawning. The task content must be a String passed into the async closure.

5. **Re-export AgentExecutor from lib.rs** if not already accessible (check if session module re-exports are sufficient).

**Important:**
- Do NOT change the scripted session path — only modify the `else` (script=None) branch
- The `git` clone is already available in the spawn closure — pass it if AgentExecutor needs it
- Keep the same JoinSet<(String, Result<SessionRunState, String>)> signature
- Maintain the same error handling pattern (map errors to SessionRunState::Failed)
  </action>
  <verify>
    - `cargo check -p smelt-core` passes
    - `cargo clippy -p smelt-core -- -D warnings` passes
    - Existing orchestrator tests still pass: `cargo test -p smelt-core --lib orchestrate`
  </verify>
  <done>
    - Agent sessions (script=None) dispatch to AgentExecutor in the orchestrator
    - Preflight check for `claude` binary runs before worktree creation
    - task_file content is resolved before spawning
    - Timeout from manifest is passed to AgentExecutor
    - Existing scripted session behavior is unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire AgentExecutor into SessionRunner + CLI preflight message</name>
  <files>
    crates/smelt-core/src/session/runner.rs
    crates/smelt-cli/src/commands/orchestrate.rs
  </files>
  <action>
**SessionRunner (runner.rs):**

Modify `SessionRunner::run_manifest()` to dispatch to `AgentExecutor` when `session.script` is None:

1. Replace the current `None` branch (which returns immediate Completed with no commits) with:
   - Resolve `claude` binary via `resolve_claude_binary()` (or accept it as a parameter)
   - Create AgentExecutor with the worktree path and a default log path
   - Call `execute()` with task, file_scope, and a new CancellationToken (no orchestrator-level cancel in SessionRunner — create a dummy token)
   - Map the SessionResult through the same state update logic

   Note: SessionRunner is used by `smelt session run` (standalone, non-orchestrated). For v0.1.0, it's acceptable to have a simpler integration here — no timeout (or use session_def.timeout_secs), no cancellation (or a never-cancelled token).

2. Handle the case where `claude` is not installed:
   - If resolve_claude_binary() fails, return a Failed SessionResult with a clear message
   - Do NOT abort the entire manifest — just fail that one session

**CLI orchestrate.rs:**

1. Add a startup message when agent sessions are detected:
   - After manifest load, scan for script=None sessions
   - If any exist, log/print: "Detected N agent session(s) — using Claude Code backend"
   - If `claude` is not on PATH, print an error and exit before starting orchestration

2. Ensure the `--no-ai` flag does NOT disable agent sessions (it only disables AI conflict resolution). Agent sessions and AI conflict resolution are separate features.

**Important:**
- Do NOT modify the existing test for `run_manifest_session_without_script_returns_completed` — this test will need updating since the behavior changes. Add a `#[ignore]` attribute with a comment explaining it requires `claude` on PATH, or restructure the test to mock the binary. Best approach: make AgentExecutor's binary path configurable so tests can point to a stub script.
- For the SessionRunner test compatibility: consider adding a `--scripted-only` or similar mechanism, OR accept that the existing test will need adjustment. The simplest approach: if `resolve_claude_binary()` fails in SessionRunner, fall back to the old behavior (Completed with no commits) and log a warning. This preserves backward compatibility for scripted-only usage.
  </action>
  <verify>
    - `cargo check -p smelt-core -p smelt-cli` passes
    - `cargo clippy -- -D warnings` passes
    - `cargo test -p smelt-core --lib session::runner` — existing tests still pass (backward compatibility)
    - `cargo test -p smelt-cli` — existing CLI tests still pass
  </verify>
  <done>
    - SessionRunner dispatches to AgentExecutor for script=None sessions
    - CLI prints informational message when agent sessions are detected
    - Backward compatibility maintained: scripted-only manifests work as before
    - Claude not installed gracefully degrades to Failed for that session
  </done>
</task>

</tasks>

<verification>
- `cargo build --workspace` succeeds
- `cargo clippy --workspace -- -D warnings` passes
- `cargo test --workspace` — all existing tests pass (no regressions)
- Orchestrator correctly dispatches script=None sessions to AgentExecutor
- Scripted sessions are completely unchanged
</verification>

<success_criteria>
- Sessions without `script` field launch Claude Code via AgentExecutor
- Preflight check prevents cryptic errors when `claude` is not installed
- Timeout from manifest `timeout_secs` is enforced on agent sessions
- Existing scripted session tests pass without modification
- CLI provides clear feedback about agent session detection
</success_criteria>

<output>
After completion, create `.planning/phases/10-real-agent-sessions/10-02-SUMMARY.md`
</output>
